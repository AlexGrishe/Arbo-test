import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'arboStar-test',
  webDir: 'dist/arbo-star-test',
  bundledWebRuntime: false
};

export default config;
